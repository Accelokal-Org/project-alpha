# project-alpha

Interactive Next.js wireframe for EduArchive, a school records and student management system.

## Local development

```bash
npm install
npm run dev
```

## Deploy to Vercel

Import this repository in Vercel. The framework preset should be detected as
Next.js, with `npm run build` as the build command. No environment variables
are required for the interactive wireframe.
